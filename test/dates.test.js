import test from 'node:test';import assert from 'node:assert/strict';import {exactDay,easternDay} from '../src/analytics/dates.js';import {slateService} from '../src/analytics/slates.js';
test('prop date accepts user date and today in Eastern time with DST',()=>{assert.equal(exactDay('09-10-26'),'2026-09-10');assert.equal(exactDay('today',Date.parse('2026-09-11T02:00:00Z')),'2026-09-10');assert.equal(easternDay('2026-09-11T00:20:00Z'),'2026-09-10');assert.equal(easternDay('2026-09-13T17:00:00Z'),'2026-09-13');assert.equal(easternDay('2026-12-11T04:30:00Z'),'2026-12-10');assert.throws(()=>exactDay('2026-02-30'));});
test('exact-day prop retrieval includes evening ET games and excludes later slate games',async()=>{const events=[{id:'tonight',commence_time:'2099-09-11T00:20:00Z'},{id:'later',commence_time:'2099-09-13T17:00:00Z'},{id:'nextday',commence_time:'2099-09-11T04:01:00Z'}];const calls=[];const svc=slateService({events:async()=>events,odds:async(s,id)=>{calls.push(id);return []; }},{capture:()=>{}});const r=await svc('nfl','receiving',{from:'09-10-99',exactDate:true});assert.equal(r.games,1);assert.deepEqual(calls,['tonight']);calls.length=0;const empty=await svc('nfl','receiving',{from:'2099-09-12',exactDate:true});assert.equal(empty.games,0);assert.deepEqual(calls,[]);});
